# STOREFRONT-PLAN-V6 — Adversarial Critique (Cycle 6)

**Critic:** CRITIC agent, plan-convergence loop · **Date:** 2026-09-11
**Target:** STOREFRONT-PLAN-V6.md · **Ground truth:** STOREFRONT-PLAN-V5-CRITIQUE.md (H7/L10 definitions) + V1–V5 critiques + orchestrator-verified facts (trusted) + fresh verification this cycle (full policy-statement extraction from `20260407074308`, `20260407150827`, `20260408191701`, `20260408205133`, `20260412170009`, `20260619164824`, `20260620172022`; full read of `20260620172022`; repo-wide pathao-policy scan (134 migrations); all-policy scan on `categories`/`product_categories`; post-drop anon-policy scan on all ten tables; `src/storefront` category-usage scan (20 files); multiplicity-aware V5↔V6 full-text diff)

---

## Verdict

**REVISE — 2 actionable findings (0 HIGH, 2 MEDIUM).**

Both cycle-5 fixes landed exactly as specified. H7 is fully resolved: fact 6 now carries the true four-migration attribution (`20260407074308` ×5 — orders, order_items, customers, products, stores, "exactly five"; `20260407150827` ×2 — order_timeline, order_payments; `20260408191701` ×1 — product_variations; `20260408205133` ×2 — categories, product_categories), verified statement-by-statement against all four files; §9.1's drop block now contains all ten `DROP POLICY IF EXISTS` lines with names matching `20260412170009` lines 2–11 exactly; the false "pathao tables are separately public read-only by design" comment is replaced with the true live state AND the four idempotent tighten drops; §9.6's zero-anon assertion covers every dropped table (nine named + `product_variations` via its exactly-one-SELECT-only clause — the full ten); §1/§3/§9.5/§10.1/§10.3/§14.3 are aligned; and V6's two new §9.1 claims verify clean ("zero category references under src/storefront" — true, 0/20 files; no post-`20260412170009` anon policy on any of the ten tables except the products SELECT-only). L10 is resolved: all three non-idempotent statements (`uq_storefront_pages_one_home`, `storefront_page_sections`, `idx_sp_sections_page`) now carry `IF NOT EXISTS`, every statement in migration 1 re-runs safely (ADD COLUMN IF NOT EXISTS ×6, CREATE … IF NOT EXISTS ×3, CREATE OR REPLACE fn, DROP IF EXISTS + CREATE trigger ×2, ADD VALUE IF NOT EXISTS, ON CONFLICT backfill), migration 2 is guard-no-op, and the line-145 claim is now genuinely true. The V5↔V6 diff is surgical — 23 runs, every changed line maps to the H7/L10 fix sites, headers, or §14.5; §5–§8 byte-identical to the V5-verified state; effort arithmetic unchanged.

But the fresh-eyes sweep found that the newly added pathao re-create block — a verbatim mirror of `20260412170009`'s tail as the cycle-5 required resolution specified — is wrong in both reachable states: **on a fresh `db reset` the migration ERRORS at its first `CREATE POLICY`** (the April migration already created those exact policy names on any DB where it succeeded), and **the `pathao_stores` anon-read re-create reverses a deliberate June-2026 tightening** (`20260620172022`: "pathao_stores: remove public read") — a reversal the plan's own §9.6 assertion and §10.1 row then enforce as the target state. The plan rewrote the false pathao comment (H7's part 3) correctly, but the replacement SQL encodes an April snapshot of the tighten, not the migration chain's amended end-state.

---

## HIGH findings

None.

---

## MEDIUM findings

### M14 — The pathao re-creates are not idempotent and fail on first application to a fresh database: `20260412170009`'s own tail already created every one of those policy names, so `checkout_atomic_idempotency.sql` errors at line 479 on any `db reset` (and on any re-run), falsifying the "Re-run, idempotently:" claim in the same comment block
- **Plan section:** §9.1 lines 479–486 (8 plain `CREATE POLICY` statements) vs. the claim at line 474 ("Re-run, idempotently:") and the line-453 framing ("harmless where already gone"); §9.6 line 630 (verify assertions presuppose a migrated DB)
- **Defect:** `20260412170009` lines 20–30 created `"Anyone can read pathao_cities/zones/areas/stores"` and `"Authenticated can manage pathao_cities/zones/areas/stores"`. On a fresh `db reset` — or any new project/branch the chain is replayed into — that migration succeeds, so all eight names exist when Phase 6's migration runs later (timestamp order), and the first plain `CREATE POLICY "Anyone can read pathao_cities"` (line 479) errors with `policy … already exists`. The migration therefore cannot apply to a fresh database at all, breaking the dev/CI substrate every §9.6 fixture (RPC fixtures, race test, verify file) runs on, and breaking any re-run. This is L10's defect class but strictly stronger: not merely a false re-run claim — a first-run failure in a state the plan itself explicitly contemplates ("harmless where already gone" describes exactly the fresh-reset state where the April drop succeeded). The plan's own precedent (fact 9: "A `CREATE TABLE storefront_pages` migration would fail on deploy") treats apply-time failure in a reachable state as a real defect. Note the ten anon-policy drops (lines 462–471) are fine — `IF EXISTS` — and the April tail's own plain CREATEs were safe because nothing earlier created those names; the Phase 6 mirror runs *after* the original and inherits the collision. PostgreSQL has no `CREATE POLICY IF NOT EXISTS`.
- **Evidence:** `20260412170009` lines 14–30 (4 drops + 8 creates, names verbatim identical to plan lines 475–486); plan lines 472–486; plan line 474 claim; plan line 453 claim; §4.1 lines 102–106/116–119 (the plan's own DROP-IF-EXISTS-then-CREATE idiom for self-owned objects); repo precedent `20260614043218` lines 15–16 (DROP IF EXISTS + CREATE for a re-created policy).
- **Required resolution:** Precede each of the eight `CREATE POLICY` statements with `DROP POLICY IF EXISTS "<same name>" ON public.<same table>;` (the §4.1 trigger idiom — on live the drop is a no-op and the create is the tighten; on fresh the drop+create is a byte-identical re-create). This makes the block genuinely "harmless where already gone" and makes the line-474 claim true. (This resolution also presupposes M15's carve-out — the `pathao_stores` read policy must NOT be re-created at all, so its guard/create lines are deleted rather than guarded.)

### M15 — Re-creating `"Anyone can read pathao_stores"` reverses the deliberate `20260620172022` tightening ("pathao_stores: remove public read") in both reachable states, and §9.6's "each pathao_* table has exactly its SELECT-only + authenticated-manage pair" assertion plus §10.1's pathao row enforce that regression as the verified target state
- **Plan section:** §9.1 lines 472–478 + 485 (tighten comment, drops, pathao_stores read re-create), §9.6 line 630 (pair assertion), §10.1 line 661 (pathao row), fact 6 line 27 (pathao sentence, "until Phase 6 re-runs it (§9.1)"), §10.3 line 676 ("plus re-running that migration's pathao_* tighten"), §14.5 H7 row (line 794)
- **Defect:** `20260620172022` (June 2026, later than the April tighten) states "-- 2. pathao_stores: remove public read" and executes `DROP POLICY IF EXISTS "Anyone can read pathao_stores" ON public.pathao_stores;` — a deliberate access-control decision that `pathao_stores` (courier store mappings) must not be publicly readable, leaving it authenticated-manage-only. The V6 plan, mirroring `20260412170009`'s April tail verbatim, re-creates `"Anyone can read pathao_stores" FOR SELECT USING (true)` (line 485): on a fresh chain it undoes the June decision outright, and on live it grants a public read the chain's latest state says should not exist (on live today the table is anon-writable via `FOR ALL` PUBLIC "Public access", so Phase 6 is still a net tightening on live — but it deliberately re-establishes an exposure the recorded migration decision removed, unacknowledged and un-owned, in the very section whose purpose is a security closure). The plan's verification then locks the wrong state in: §9.6 line 630 asserts "each `pathao_*` table has exactly its SELECT-only + authenticated-manage pair" — an assertion that can only pass if the June decision is defeated — and §10.1 line 661 states the same target. The plan reads the tighten's provenance (H7's point) but not its June amendment; the same block's own logic for categories ("If a future anon need appears, add a named SELECT-only policy as a deliberate decision", lines 460–461) applies verbatim to pathao_stores. No storefront need exists: the shipping quote is service-role (§8.2/M1) and `src/storefront` has zero pathao references.
- **Evidence:** `20260620172022` lines 9–10 (full file read: "-- 2. pathao_stores: remove public read" + `DROP POLICY IF EXISTS "Anyone can read pathao_stores"`; the same file also re-creates the products SELECT-only policy with column grants excluding `cost_price` — see appendix); repo-wide scan: `"Anyone can read pathao"` appears in exactly two migrations — `20260412170009` (creates ×4) and `20260620172022` (drops pathao_stores only); `"Authenticated can manage pathao"` appears only in `20260412170009`; plan lines 485, 630, 661, 27, 676.
- **Required resolution:** (1) Delete the `"Anyone can read pathao_stores"` re-create (line 485) and its guard from M14's fix; keep the guarded `"Authenticated can manage pathao_stores"` create (live needs it — the tighten never ran there). Add a one-line comment citing `20260620172022` ("pathao_stores: remove public read — the April tighten re-runs as amended; pathao_stores ends with zero anon policies"). (2) Rewrite §9.6 line 630's pathao clause: cities/zones/areas = SELECT-only + authenticated-manage pair; **pathao_stores = authenticated-manage only, zero anon policies**. (3) Align §10.1 line 661, fact 6's pathao sentence (line 27), §10.3 line 676, and the §14.5 H7 row to the amended scope ("the tighten as amended by `20260620172022`"). Effort: one block edit inside Phase 6's existing 7.5–9.5 d.

---

## Resolution table (cycle-5 findings → V6 verdicts)

| # | Verdict | Justification (one line) |
|---|---|---|
| H7 | **RESOLVED** | Fact 6's attribution now matches the migrations statement-for-statement (×5/×2/×1/×2 across `20260407074308`/`20260407150827`/`20260408191701`/`20260408205133`, [H7] marker present, false "all eight" claim gone); §9.1 drops all ten policies with names identical to `20260412170009` lines 2–11; the false "read-only by design" comment is replaced with the true live state and the four tighten drops added (required-resolution option 1, plus the honest comment); §9.6 covers every dropped table (nine named + product_variations via its exactly-one clause, "the full ten-table set" stated) and adds the pathao pair check; §1 (line 10), §3 Phase-6 row (line 56), §9.5 (line 626), §10.1 (lines 659–661), §10.3 exception #1 (line 676), and §14.3's H5 row (line 757, "(Attribution corrected in V6 by H7…)") all aligned; V6's new supporting claims verified fresh (zero `src/storefront` category references: 0/20 files; zero post-drop anon policies on the ten tables except the products SELECT-only; `20260619164824` citation accurate). The residual defects in the *new* pathao re-create SQL are filed as M14/M15, not scored against H7. |
| L10 | **RESOLVED** | All three named statements now carry `IF NOT EXISTS` (lines 82–83, 85, 95, flagged `-- L10`); a full statement inventory of migration 1 finds zero remaining non-idempotent statements (ADD COLUMN IF NOT EXISTS ×6, three guarded CREATEs, CREATE OR REPLACE fn, DROP IF EXISTS + CREATE trigger ×2, ADD VALUE IF NOT EXISTS, ON CONFLICT backfill); migration 2's guard makes it a no-op re-run; the line-145 claim is re-scoped with the exact reason and is now genuinely true — verified against the L10 required resolution's option 1. |

**RESOLVED: 2 · PARTIALLY: 0 · NOT: 0** — plus **2 new actionable findings (M14 MEDIUM, M15 MEDIUM)**.

---

## Fresh-eyes sweep (cycle-6 delta audit)

- **V5↔V6 diff (multiplicity-aware, exact-line LCS):** 23 runs, every one mapping to: title/header (lines 1, 3–4), §1 goal (line 10), §2 header + preamble + fact 6 (lines 18, 20, 27), §3 Phase-6 row (line 56), §4.1's three DDL lines + line-145 claim (lines 82, 85, 95, 145), §9.1 comment + drops + pathao block + M9-note citation fix (lines 446–489 — including the correct `20260407074308`→`20260408191701` correction inside the M9 note), §9.5 (line 626), §9.6 (line 630), §10.1 (lines 660–661), §10.3 (line 676), §14.3 H5 row annotation (line 757), §14.4 header ("retained"), and §14.5 (new). **Zero collateral damage** — §4.2–§4.6, §5–§8, §9.2–§9.4, §10.2, §11–§13, §14.1–§14.4 byte-identical to the V5-verified state; "retaining every cycle-1/2/3/4/5 resolution" holds.
- **M14 collision derivation:** fresh reset replays chain in timestamp order → `20260412170009` succeeds → all eight pathao policy names exist → Phase 6 (later timestamp) hits `policy "Anyone can read pathao_cities" for table "pathao_cities" already exists` at line 479. On live (trusted premise: orders anon OPEN ⇒ no statement of `20260412170009` took effect) the first run succeeds — the failure is fresh-reset/re-run only, hence MEDIUM, not HIGH.
- **M15 state matrix:** live today = `"Public access to pathao_*"` ×4 only (anon-writable; the June drop was a no-op on live since the April create never landed); fresh chain = cities/zones/areas pair + pathao_stores manage-only. Plan line 485 re-grants public read in both. Live is still net-tightened by Phase 6 (write hole closed), so the regression is against the chain's deliberate end-state and the June decision's intent — MEDIUM.
- **Ten-table closure completeness re-verified:** repo-wide scan — no migration after `20260412170009` creates any anon policy on the ten tables except `20260619164824`/`20260620172022`'s products SELECT-only (differently named, not in the drop list); `20260415001620` drops the old *authenticated* manage policies on categories/product_categories (lines 205, 245) — outside Phase 6's scope, no effect on the anon-closure claims.
- **Effort arithmetic re-verified:** 20–25 + 3.5–5.5 + 5–7.5 + 5–7 + 3.5–5 + 7.5–9.5 = 44.5–59.5 d = 8.9–11.9 wk; header = §3 = §14.5's "none" delta. M14+M15's fix (guard lines + one deleted create + assertion wording) fits inside Phase 6's existing estimate.
- **No other new V6-text defects found:** §9.2 retry contract, §9.3 H6 wiring, §9.4 Track sequencing, §9.6 fixtures, §10.1/§10.2/§10.3 remainder, §11, §12, §13 — re-read; internally consistent and unchanged from the V4/V5-verified state.

## Appendix — minor notes (not actionable)

1. **Products-policy provenance nuance:** `20260620172022` drops and re-creates `"Public can read active products"` (identical name/semantics to `20260619164824`) and adds column-level grants excluding `cost_price`. The plan's claims ("products has exactly the `20260619164824` SELECT-only policy", "which is NOT dropped") remain accurate in substance — the name originates there, the re-create is semantically identical, and Phase 6's drop list doesn't touch it. No change required; noted for completeness.
2. **`20260415001620` and categories:** that migration drops the original `"Authenticated users can manage …"` policies on categories/product_categories (presumably replacing them) — Phase 6 touches only the anon policies, so the manage-side history doesn't affect any V6 claim.
3. **M14/M15 lineage:** both defects entered with V6's new pathao block; the cycle-5 critique's required resolution specified "mirroring `20260412170009`'s own tail" and its own fresh-reset no-op claim covered only the added *drops* — the mirror was implemented as specified, but the specification missed the June amendment and the create-side collision. The findings are scored against the plan text (the loop's standard), not the instruction.
4. **Fact 11 wording** ("No equivalent anon policy on `product_variations` exists in migrations") remains acceptable in context ("equivalent" = the SELECT-only is_active-gated kind, per the sentence's own referent); unchanged from V5's non-actionable classification.
5. **M15's scope judgment:** on live, Phase 6 still closes a strictly larger hole than it opens (anon write → anon read on pathao_stores), which is why this is MEDIUM and not HIGH; the defect is the silent reversal of a recorded tightening plus a verify assertion that enforces the reversal.

## Verification method (this cycle)

- **Fresh repo verification:** per-file policy extraction from all five H7-cited migrations (counts 5/2/1/2 exact; drop-list name match ×10); complete reads of `20260412170009` (30 lines) and `20260620172022` (the pathao_stores decision); `20260619164824` full read; repo-wide scans across all 134 migrations for pathao policy statements, categories/product_categories policy history, and post-drop anon policies on the ten tables; `src/storefront` (20 files) category-usage scan (zero hits) + repo-wide `.from('categories'|'product_categories')` call-site census (38, all admin/service-role); migration-1 statement-by-statement idempotency inventory for L10.
- **Plan-text analysis:** H7/L10 resolution audits against the V5 critique's required resolutions; §9.6 assertion-coverage check (ten-table set, pathao pair); V5↔V6 multiplicity-aware diff + run-by-run site mapping (collateral-damage hunt); M14/M15 state matrices derived for live / fresh-reset / re-run under the plan's own rollback premise; effort arithmetic recomputed.
- **PostgreSQL semantics applied:** `CREATE POLICY` name collision (`policy … already exists`) on re-run and on replay-after-successor; absence of `CREATE POLICY IF NOT EXISTS`; `DROP POLICY IF EXISTS` idempotency on live-vs-fresh divergence; migration timestamp ordering on fresh reset.
